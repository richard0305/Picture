<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
 	android:background="#C7EDCC"
    android:layout_height="match_parent" >

    <ImageView
        android:id="@+id/iv_showPhoto"
        android:layout_width="200dp"
        android:layout_height="300dp"
        android:layout_centerHorizontal="true"
        android:src="@drawable/ic_launcher" />

    <TextView
        android:id="@+id/tv_content"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
       android:textColor="#F65929"
        android:padding="2dp"
        android:layout_alignBottom="@+id/iv_showPhoto"
        android:layout_alignLeft="@+id/iv_showPhoto"
        android:singleLine="true"
        android:text="刘亦菲" />

    <TextView
        android:id="@+id/tv_count"
        android:padding="2dp"
        android:layout_width="wrap_content"
        android:textColor="#042E76"
        android:layout_height="wrap_content"
        android:layout_above="@+id/tv_content"
        android:layout_alignLeft="@+id/tv_content"
        android:text="2张" />

</RelativeLayout>
